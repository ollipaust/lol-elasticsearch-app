import { accent, light, dark } from './colors'

export const theme = {
  typography: {
    fontFamily: 'Roboto, Helvetica, sans-serif',
  },
  colors: {
    primaryColor: accent,
    titleColor: light,
  },
  secondaryColor: dark,
}
